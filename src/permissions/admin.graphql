query adminUser($user_id: ID!) {
  SomeUserExists(filter: {
    id: $user_id
    role: [ADMIN]
  })
}

query adminPost($user_id: ID!) {
  SomePostExists(filter: {
    id: $user_id
    role: [ADMIN]
  })
}